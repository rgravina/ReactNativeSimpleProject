import React, { Component } from 'react';
import { TextInput, View, Text } from 'native-base';

export default class Login extends Component {
  render() {
    return (
      <View>
      </View>
    );
  }
}
